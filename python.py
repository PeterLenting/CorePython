"""
def print_message(message):
    print(message)
    
print_message("Hello World")
"""
"""
my_list = [1, 2, 3, 4, 5]

for item in my_list:
    print(item)
"""
"""
use bash to install flask: sudo pip3 install Flask
in python.py: from flask import Flask

in bash to  make a list of al the dependencies: pip3 freeze --local > requirements.txt
"""
from flask import Flask